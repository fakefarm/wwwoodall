---
layout: album
---
= link_to '/' do
  .fa.fa-home
| &nbsp;
= link_to 'pictures', '/pictures'
h1= album
- data.pictures.collections.each do |collection|
  - if collection.slug == album
    .album
      - collection['entries'].each do |photo|
        .pic = link_to((image_tag photo['url']), "#{photo['url']}")
