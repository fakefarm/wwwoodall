---
layout: album
---
= link_to '/' do
  .fa.fa-home
| &nbsp;
= link_to 'art', '/art'
h1= album
- data.art.collections.each do |collection|
  - if collection.slug == album
    .album
      - collection['entries'].each do |photo|
        .pic = link_to((image_tag photo['url']), "#{photo['url']}")
