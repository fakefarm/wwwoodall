---
title: 'blog'
image: laird.jpg
layout: layout
---

.shortcuts
  .intro-text
    h3#tag-title blog
    p.date
      | Read
      '
      = link_to 'How I blog', '/blog/how-i-blog/'

.l-container
  .l-blog
    .fade-in-element
      .scroll-on-page
        aside#scroll-on-page-top
          .blog-tags
            p Tags
            ul
              - blog_tags.each do |tag|
                li= tag
    .l-copy#container
      #blog
        - blog.articles.each do |post|
          article.type-system-traditional#article data-tag="#{post.tags.first}"
            .post
              h1= post.title
              p= post.summary
              .link=link_to post.url, class: 'read-more' do
                ' continue reading
                |
                  &rsaquo;
